import Link from "next/link";
import { getCurrentUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { money } from "@/lib/format";
import { redirect } from "next/navigation";
export default async function CartPage(){const user=await getCurrentUser();if(!user)return <main className="mx-auto max-w-xl px-4 py-24 text-center"><h1 className="text-3xl font-black">Your cart</h1><p className="mt-3 text-slate-500">Sign in to keep your cart synced across devices.</p><Link href="/auth/login" className="mt-7 inline-block rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">Sign in</Link></main>;const cart=await prisma.cart.findUnique({where:{userId:user.id},include:{items:{include:{product:{include:{images:{where:{isPrimary:true},take:1}}},variant:true}}}});const items=cart?.items??[];const subtotal=items.reduce((s,i)=>s+Number(i.price)*i.quantity,0);return <main className="mx-auto max-w-5xl px-4 py-12 lg:px-6"><div className="flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-slate-500">Shopping</p><h1 className="mt-2 text-4xl font-black">Your cart</h1></div><span className="text-sm text-slate-500">{items.length} items</span></div>{items.length===0?<div className="mt-10 rounded-3xl border border-dashed p-14 text-center"><p className="text-lg font-bold">Your cart is empty.</p><Link href="/products" className="mt-5 inline-block rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">Start shopping</Link></div>:<div className="mt-8 grid gap-6 lg:grid-cols-[1fr_330px]"><div className="space-y-3">{items.map(i=><div key={i.id} className="flex gap-4 rounded-2xl border bg-white p-4"><div className="h-24 w-24 overflow-hidden rounded-xl bg-slate-100">{i.product.images[0]&&<img src={i.product.images[0].url} alt={i.product.name} className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><h2 className="font-bold">{i.product.name}</h2><p className="mt-1 text-sm text-slate-500">Qty {i.quantity}</p><p className="mt-3 font-black">{money(Number(i.price)*i.quantity)}</p></div></div>)}</div><aside className="h-fit rounded-3xl bg-slate-950 p-6 text-white"><h2 className="text-xl font-black">Summary</h2><div className="mt-6 flex justify-between text-slate-300"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="mt-3 flex justify-between text-slate-300"><span>Shipping</span><span>Calculated at checkout</span></div><div className="my-6 border-t border-white/10"/><div className="flex justify-between text-lg font-black"><span>Total</span><span>{money(subtotal)}</span></div><Link href="/checkout" className="mt-6 block rounded-xl bg-white py-3 text-center font-bold text-slate-950">Checkout</Link></aside></div>}</main>}
